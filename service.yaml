apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
spec:
  type: NodePort   # 👈 IMPORTANT CHANGE
  selector:
    app: fastapi
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007